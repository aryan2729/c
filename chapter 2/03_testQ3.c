#include<stdio.h>

int main(){

    
    // Checking if number divisible by 97 or not ?
    int a = 97;
    printf("The value of (a%97) is %d", a%97 );  // if a is divisible by 97 then it's return 0; other wise something 

    return 0;
}