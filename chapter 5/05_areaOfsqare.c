#include<stdio.h>
#include<math.h>

int main(){

    int a = 5;
    printf("The area of this square is %f \n", pow(a,2));   // pow used for power a with power 2  cuz sqare area = a sqaure| %f cuz it returns floating no. 

    return 0;
}