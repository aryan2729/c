#include<stdio.h>

int main(){

    int a ;

    //🔥 For INPUT From user 

    scanf("%d", &a );   //🍁 INPUT use -> scanf  ND always use 🍁 ( &a ) in scanf | & -> address of operator  | %d -> int , %c -> char , %f -> float

    printf("The value of a is %d \n",a);  



 
    // task store your number by taking input from the user 
    int number ;

    scanf( "%d" , &number);   //🍁 always &a not single a 
    printf("The number of user is %d",number);




    return 0;
}