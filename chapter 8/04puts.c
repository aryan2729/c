#include<stdio.h>

int main(){

    // puts in string 

    // 🍁🍁 puts -> Used for input but after that when u do printf then after whatever u print thin it goes to next line 

    char st[30];

    gets(st);           // The entered string is stored in st!
    puts(st);           //🚀 Prints the string & places the cursor on the next line

    printf("Hello");
    

    return 0;
}