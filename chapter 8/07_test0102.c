#include<stdio.h>

int main(){

    // # ans 1 -> gets

    char st[6];

    scanf("%s", st);   //🔥🔥M.i.m(while using scanf for string we don't use &(ampercent) )


    printf("%s", st);

    return 0;
}